[project]
name = "show-guesser"
version = "0.1.0"
description = "Friends describe a frame, you guess what episode it is from"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "discord-py>=2.7.1",
]
